<div class="w-50 text-center">
% if ( defined($c->session('notifications')) && ( scalar( @{ $c->session('notifications') } ) > 0 ) ){
%   foreach my $notice ( @{$c->session('notifications')} ){
<div class="alert <% $css{$notice->{'type'}} %>" role="alert">
  <p><% $notice->{'message'} %></p>
</div>
% }
%   delete($c->session->{'notifications'});
% }
</div>

<%init>
my %css = (
    error => 'alert-danger',
    info  => 'alert-success'
);
</%init>
