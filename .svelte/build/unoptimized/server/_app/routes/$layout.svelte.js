/* src/routes/$layout.svelte generated by Svelte v3.32.0 */
import {
	create_ssr_component,
	missing_component,
	validate_component
} from "../../_snowpack/pkg/svelte/internal.js";

import Nav from "../../_components/Nav.svelte.js";
import Notifications from "../../_components/Notifications.svelte.js";

async function preload(page, session) {
	const { user } = session;

	if (!user && page.path != "/login") {
		return this.redirect(302, `http://${page.host}/login`);
	}
}

const $layout = create_ssr_component(($$result, $$props, $$bindings, slots) => {
	let { segment } = $$props;
	if ($$props.segment === void 0 && $$bindings.segment && segment !== void 0) $$bindings.segment(segment);

	return `${!segment || segment != "login"
	? `${validate_component(Nav || missing_component, "svelte:component").$$render($$result, { segment }, {}, {})}`
	: ``}

<div class="${"col-sm-12 col-md-6"}">${validate_component(Notifications || missing_component, "svelte:component").$$render($$result, {}, {}, {})}</div>

${slots.default ? slots.default({}) : ``}`;
});

export default $layout;
export { preload };
//# sourceMappingURL=$layout.svelte.js.map
