#:kivy 1.0

<Background>:
    canvas:
        Rectangle:
            source: "{}{}".format("digital_dash_gui/static/imgs/Background/", app.background_source ) if app.background_source else ''
            size: self.size
            pos: self.pos
