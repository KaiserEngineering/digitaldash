#:kivy 1.0

<Background>:
    canvas:
        Rectangle:
            source: "{}{}".format(app.working_path+"/static/imgs/Background/", app.background_source ) if app.background_source else ''
            size: self.size
            pos: self.pos
